(function () {
	"use strict";

	let CCDIK = function (store, action) {

	};

	CCDIK.prototype.translate = function (name, entity, initialpos, prepos, curpos, initialVal, isDone) 
	{
		
	}

	upaint.CCDIK = CCDIK;

}());
